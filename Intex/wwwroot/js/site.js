// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.


/*
window.onload = function () {
    var selCounty = sessionStorage.getItem("SelCounty");
    $('#sort-county').val(selCounty);
}
$('#sort-county').change(function () {
    var selVal1 = $(this).val();
    sessionStorage.setItem("SelCounty", selVal1);
});

window.onload = function () {
    var selItem = sessionStorage.getItem("SelCity");
    $('#sort-city').val(selItem);
}
$('#sort-city').change(function () {
    var selVal = $(this).val();
    sessionStorage.setItem("SelCity", selVal);
});

var resetter = document.getElementById("mySumForm");

function MyReset() {
    resetter.reset();
}
*/